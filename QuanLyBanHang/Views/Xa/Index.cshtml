@model IEnumerable<QuanLyBanHang.Models.Xa>

@{
    ViewBag.Title = "Quản lý Xã";
}

<style>
    .search-wrapper {
        position: relative;
        width: 100%;
        max-width: 400px;
    }

        .search-wrapper input {
            padding-left: 38px !important;
            border-radius: 8px;
            background: #f8f9fa;
        }

        .search-wrapper i {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            pointer-events: none;
        }

    .table {
        overflow-x: auto;
        white-space: nowrap;
        width: 100%;
    }

    .table-hover tbody tr:hover {
        background-color: #f5faff;
        transition: 0.2s;
    }
</style>

<div class="container">
    <h4 class="mb-3 text-success">
        Danh sách Xã
    </h4>

    <!--  Form -->
    <form id="Form" method="get" class="d-flex flex-wrap align-items-center justify-content-between mb-4 gap-3">
        <div class="search-wrapper flex-grow-1">
            <i class="fa fa-search"></i>
            <input id="searchBox" type="text" name="search" value="@ViewBag.Search" class="form-control" placeholder="Tìm tên, mã xã...">
        </div>

        <div class="d-flex flex-wrap align-items-center gap-2">
            <div class="d-flex align-items-center gap-2">
                <label class="w-100 text-end opacity-50"> <i class="fa fa- me-2"></i>Tỉnh/Thành phố:</label>
                 <select asp-items="ViewBag.TinhList" name="maTinh" class="form-control w-100" id="maTinhSelect">
                <option value="">-- Chọn tỉnh --</option>
            </select>
            </div>

            <button type="button" id="btnClear" class="btn btn-secondary"><i class="fa-solid fa-rotate-left"></i>Reset</button>
            <a href="@Url.Action("Create", "Xa")" class="btn btn-success"><i class="fa-solid fa-circle-plus"></i> Add</a>
        </div>
    </form>

   <div class="table">
        <table class="table table-bordered table-hover align-middle text-center shadow-sm">
            <thead class="table-success">
                <tr>
                    <th class="w-15">Mã Xã</th>
                    <th class="w-25">Tên Xã</th>
                    <th class="w-15">Mã Tỉnh</th>
                    <th class="w-25">Tên Tỉnh</th>
                    <th class="w-20">Hành động</th>
                </tr>
            </thead>
            <tbody class="text-start">
                @foreach (var item in Model)
                {
                    <tr class="clickable-row"
                        data-href="@Url.Action("Details", "Xa", new { id = item.MaXa })"
                        data-tenxa="@item.TenXa"
                        data-maxa="@item.MaXa"
                        data-matinh="@item.MaTinh"
                        data-tentinh="@item.TenTinh">


                        <td>@item.MaXa</td>
                        <td>@item.TenXa</td>
                        <td>@item.MaTinh</td>
                        <td>@item.TenTinh</td>

                        <td class="flex justify-content-between text-center">
                            <a class="btn btn-info btn-sm text-white" href="@Url.Action("Details", "Xa", new { id = item.MaXa })">
                                <i class="fa-solid fa-eye"></i> Chi tiết
                            </a>
                            <a class="btn btn-warning btn-sm text-white" href="@Url.Action("Edit", "Xa", new { id = item.MaXa })">
                                <i class="fa-solid fa-pen-to-square"></i> Sửa
                            </a>
                            <a class="btn btn-danger btn-sm" href="@Url.Action("Delete", "Xa", new { id = item.MaXa })">
                                <i class="fa-solid fa-trash"></i> Xóa
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
   </div>
</div>

@section Scripts {
    <script>
        // --- Hàm lọc bảng ---
        function filterTable() {
            let search = document.getElementById("searchBox").value.toLowerCase().trim();
            let tinh = document.getElementById("maTinhSelect").value;

            document.querySelectorAll("tbody tr").forEach(row => {
                let tenXa   = row.dataset.tenxa.toLowerCase();
                let maXa    = row.dataset.maxa.toLowerCase();
                let maTinh  = row.dataset.matinh;
                let tenTinh = row.dataset.tentinh.toLowerCase();

                let matchSearch =
                    tenXa.includes(search) ||
                    maXa.includes(search) ||
                    tenTinh.includes(search) ||
                    maTinh.includes(search);

                let matchTinh =
                    tinh === "" || maTinh === tinh;

                row.style.display = (matchSearch && matchTinh) ? "" : "none";
            });
        }

        // --- Sự kiện ---
        document.getElementById("searchBox").addEventListener("input", filterTable);
        document.getElementById("maTinhSelect").addEventListener("change", filterTable);

        // --- Nút reset ---
        document.getElementById("btnClear").addEventListener("click", function () {
            document.getElementById("searchBox").value = "";
            document.getElementById("maTinhSelect").value = "";
            filterTable();
        });

        // clickable row giữ nguyên
        document.addEventListener("DOMContentLoaded", function () {
            document.querySelectorAll(".clickable-row").forEach(row => {
                row.addEventListener("click", function () {
                    window.location = this.dataset.href;
                });
            });
        });
    </script>
}
