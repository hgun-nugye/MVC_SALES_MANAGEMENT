@model IEnumerable<QuanLyBanHang.Models.KhachHangDetailView>

@{
    ViewBag.Title = "Quản lý Khách hàng";
}

<style>
   
    .search-card {
        background: white;
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .form-control, .form-select {
        border-radius: 8px;
        border: 1.5px solid #dee2e6;
        padding: 0.65rem 0.85rem;
    }

    .form-control:focus, .form-select:focus {
        border-color: #198754;
        box-shadow: 0 0 0 0.25rem rgba(25, 135, 84, 0.15);
    }

    .customer-table {
        background: white;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .table thead {
        background-color: #f8f9fa;
        border-bottom: 2px solid #e9ecef;
    }

    .table th {
        font-weight: 600;
        color: #495057;
        text-transform: uppercase;
        font-size: 0.85rem;
        letter-spacing: 0.5px;
        padding: 1rem;
    }

    .table td {
        padding: 1rem;
        vertical-align: middle;
    }

    .customer-img {
        width: 45px;
        height: 45px;
        object-fit: cover;
        border-radius: 10px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .btn-action {
        width: 32px;
        height: 32px;
        padding: 0;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        transition: all 0.2s;
    }

    .badge-gender {
        padding: 0.5em 0.8em;
        border-radius: 6px;
        font-weight: 500;
    }

    .clickable-row {
        cursor: pointer;
        transition: background-color 0.2s;
    }

    .clickable-row:hover {
        background-color: #f8fbf9 !important;
    }

    .search-wrapper {
        position: relative;
    }

    .search-wrapper i {
        position: absolute;
        left: 12px;
        top: 50%;
        transform: translateY(-50%);
        color: #6c757d;
    }

    .search-wrapper input {
        padding-left: 2.5rem;
    }
</style>

<div class="container-fluid py-4">
    <div class="page-header d-flex justify-content-between align-items-center">
        <h4 class="mb-0 fw-bold">
            <i class="fa-solid fa-users-gear me-2"></i>
            Quản lý Khách hàng
        </h4>
        <a href="@Url.Action("Create", "KhachHang")" class="btn btn-light text-success fw-bold px-4">
            <i class="fa-solid fa-plus-circle me-2"></i> Thêm
        </a>
    </div>

    <!-- Filter Section -->
    <div class="filter-card">
        <form id="Form" method="get" class="row g-3 align-items-end">
            <div class="col-lg-5">
                <label class="form-label small fw-bold text-muted text-uppercase">Tìm kiếm nhanh</label>
                <div class="search-input-group">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    <input id="searchBox" type="text" name="search" value="@ViewBag.Search"
                           class="form-control" placeholder="Tìm theo tên, email, số điện thoại...">
                </div>
            </div>
            <div class="col-lg-4">
                <label class="form-label small fw-bold text-muted text-uppercase">Tỉnh / Thành phố</label>
                <select asp-items="ViewBag.TinhList" id="tinh" name="tinh" class="form-select">
                    <option value="">-- Tất cả --</option>
                </select>
            </div>
            <div class="col-lg-3 d-flex gap-2">
                <button type="button" id="btnClear" class="btn btn-outline-secondary btn-clear">
                    <i class="fa-solid fa-rotate-left"></i>
                </button>
            </div>
        </form>
    </div>

    <!-- Table Section -->
    <div class="customer-table">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th class="ps-4">Khách hàng</th>
                        <th>Liên hệ</th>
                        <th>Địa chỉ</th>
                        <th class="text-center">Tên đăng nhập</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr class="clickable-row" data-href="@Url.Action("Details", "KhachHang", new { id = item.MaKH })">
                                <td class="ps-4">
                                    <div class="d-flex align-items-center gap-3">
                                        <img src="@(string.IsNullOrEmpty(item.AnhKH) ? Url.Content("~/images/default-user.png") : Url.Content("~/images/customers/" + item.AnhKH))" 
                                             class="customer-img" alt="@item.TenKH">
                                        <div>
                                            <div class="fw-bold text-dark">@item.TenKH</div>
                                            <div class="text-muted small">@item.MaKH</div>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <div><i class="fa-solid fa-phone-volume me-2 text-muted small"></i>@item.DienThoaiKH</div>
                                    <div class="text-muted small"><i class="fa-solid fa-envelope me-2"></i>@item.EmailKH</div>
                                </td>
                                <td>
                                    <div class="text-truncate" style="max-width: 250px;" title="@item.DiaChiKH, @item.TenXa, @item.TenTinh">
                                        <i class="fa-solid fa-location-dot me-2 text-muted small"></i>
                                        @item.DiaChiKH, @item.TenXa, @item.TenTinh
                                    </div>
                                </td>
                                <td class="text-center">
                                    <span class="badge bg-light text-dark border font-monospace">@item.TenDNKH</span>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center py-5 text-muted">
                                <i class="fa-solid fa-users-slash fa-3x mb-3 opacity-25"></i>
                                <p>Không tìm thấy khách hàng nào phù hợp.</p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // tinh vẫn submit ngay khi chọn
        $("#tinh").on("change", function () {
            $("#Form").submit();
        });

        // clear 
        $("#btnClear").on("click", function () {
            $("#searchBox").val("");
            $("#tinh").val("");
            $("#Form").submit();
        });

        document.addEventListener("DOMContentLoaded", function () {
            document.querySelectorAll(".clickable-row").forEach(row => {
                row.addEventListener("click", function (e) {
                    if (e.target.closest("a, button, .badge")) return;
                    window.location = this.dataset.href;
                });
            });
        });
    </script>
}
